# Optional Docker Compose overrides.
# The app starts without this file by using defaults from docker-compose.yml.

PORT=8787
LOG_LEVEL=info

# Optional advanced overrides:
APP_CONFIG_PATH=/app/config/cameras.json
APP_RECORDINGS_DIR=/app/recordings
APP_LOGS_DIR=/app/logs
APP_LOG_LEVEL=
